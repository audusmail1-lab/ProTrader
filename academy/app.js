import { videos, lessons } from './content.js';
import { intro, practice, glossary, bindPractice, teachingPacket } from './practice.js';
const main=document.querySelector('main');
document.querySelector('.skip').addEventListener('click',e=>{e.preventDefault();main.focus();main.scrollIntoView()});
const state={lesson:0,complete:new Set(),questions:[],filter:'All',registered:false};
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const button=(text,href,secondary=false)=>`<a class="button ${secondary?'secondary':''}" href="${href}">${text} <span>↗</span></a>`;
const eyebrow=text=>`<p class="eyebrow">${text}</p>`;
const notice='<div class="notice">Classroom preview · Sample learning space, no sign-in or private access. Practice progress and questions last only until this page is refreshed.</div>';
function videoCard(v){return `<article class="video-card"><button class="thumbnail thumb-${v.id}" data-video="${v.id}" aria-label="Play ${v.title}"><span class="mini-brand">PRO TRADER / FIELD NOTES</span><span class="thumb-title">${v.title}</span><span class="play">▶</span><span class="duration">${v.duration}</span></button><div class="card-body"><span class="eyebrow">${v.topic}${v.id===4?" / OPTIONAL · DESKTOP":v.id===2||v.id===5||v.id===6?" / AFTER THE BASICS":" / GUIDED BASICS"}</span><h3>${v.title}</h3><p>${v.summary}</p><button class="text-button" data-video="${v.id}">Watch & read guide ↗</button></div></article>`}
function home(){return `<section class="hero"><div class="hero-copy">${eyebrow('LIVE LEARNING. DELIBERATE PRACTICE.')}<h1>A clearer path<br>from chart<br>to <em>understanding.</em></h1><p>Learn trading basics in live evening classes, then practice using the Pro Trader trading workspace. Paper trading means practicing with simulated money.</p><div class="actions">${button('New to trading? Start here','#start')}${button('Explore the lessons','#classroom',true)}</div><div class="hero-note"><span>01</span> Learn together <b>/</b> Practice with purpose <b>/</b> Review your process</div></div><div class="hero-visual"><div class="visual-top"><span>INSIDE PRO TRADER</span><span>THE 30-SECOND GUIDE</span></div><video controls playsinline preload="metadata" src="${videos[0].url}#t=0.1" aria-label="Pro Trader trade ticket walkthrough"></video><div class="visual-bottom"><div><small>START WITH THE TOOLS</small><h3>Know the ticket.<br>Understand the numbers.</h3></div><button class="circle" data-video="0" aria-label="Open trade ticket guide">↗</button></div></div></section><section class="ribbon"><span>Instructor-led evening classes</span><span>Two starting paths</span><span>Paper trading throughout</span><span>Pro Trader, explained</span></section><section><div class="section-head"><div>${eyebrow('YOUR STARTING POINT')}<h2>Different experience.<br>The same thoughtful process.</h2></div><p>Start where you are. Join shared classes with an exercise that fits your experience.</p></div><div class="paths"><article class="path"><span class="path-num">01 / FOUNDATIONS</span><h3>“I’m new to trading.”</h3><p>Get comfortable with charts, basic terms and the tools before practicing a complete workflow.</p><ul><li>Read a chart and find the key controls</li><li>Explain a paper trade ticket</li><li>Build a simple review habit</li></ul><a href="#start">Start with foundations ↗</a></article><article class="path"><span class="path-num">02 / WORKFLOW</span><h3>“I trade, but want guidance.”</h3><p>Connect what you already know to Pro Trader and bring more structure to your practice.</p><ul><li>Map your current process to the app</li><li>Compare analysis with chart context</li><li>Work through optional deeper exercises</li></ul><a href="#enroll?path=practicing">Build on your experience ↗</a></article></div></section><section class="week-section"><div>${eyebrow('THE FIRST WEEK')}<h2>Understand the tools.<br>Practice the whole workflow.</h2><p>Seven proposed sessions, with space to ask questions and repeat the steps that need more time. Start dates and evening times will be confirmed.</p>${button('See the week-one plan','#classroom',true)}</div><div class="week-list">${lessons.map((l,i)=>`<a href="#lesson/${i}"><span>0${i+1}</span><h3>${l.title}</h3><span>↗</span></a>`).join('')}</div></section><section><div class="section-head"><div>${eyebrow('WATCH. UNDERSTAND. TRY.')}<h2>Small lessons.<br>Useful next steps.</h2></div>${button('All seven explainers','#library',true)}</div><div class="video-grid">${[videos[1],videos[0],videos[5]].map(videoCard).join('')}</div></section><section class="closing"><div>${eyebrow('A CLASSROOM, NOT JUST A CONTENT LIBRARY')}<h2>Learn with guidance.<br>Leave with a process.</h2><p>Live classes are at the heart of the academy. A supporting self-paced Pro Trader course is being developed.</p></div>${button('Explore enrollment','#enroll')}</section>`}
function library(){return `<section class="page-heading">${eyebrow('PRO TRADER / FIELD NOTES')}<h1>Your tools,<br><em>made clearer.</em></h1><p>Short demonstrations to support your classes. Start with the beginner introduction, then explore charts, tickets and review before the analysis tools.</p></section><section class="library-section"><div class="beginner-banner"><div><h3>Your recommended starting point</h3><p>Introduction → Charts → Tickets → Journal → Analysis. Shortcuts are optional desktop tips.</p></div>${button("Open introduction","#start")}</div><div class="filters" role="group" aria-label="Filter videos by topic">${['All',...new Set(videos.map(v=>v.topic))].map(t=>`<button data-filter="${t}" aria-pressed="${state.filter===t}" class="${state.filter===t?'selected':''}">${t}</button>`).join('')}</div><div class="video-grid">${[videos[1],videos[0],videos[3],videos[2],videos[5],videos[6],videos[4]].filter(v=>state.filter==='All'||v.topic===state.filter).map(videoCard).join('')}</div><p class="muted">Actual Pro Trader interface with illustrative paper data. Six narrated clips with captions; Oracle is caption-only.</p></section>`}
function classroom(){const next=state.complete.has(state.lesson)?lessons.findIndex((_,i)=>!state.complete.has(i)):state.lesson;const resume=next<0?6:next;const finished=state.complete.size===lessons.length;const l=lessons[resume];return `<section class="page-heading compact">${eyebrow('MY CLASSROOM / WEEK 01')}<h1>Make progress,<br><em>one practice at a time.</em></h1>${notice}</section><section class="dashboard"><div class="learning-main"><div class="beginner-banner"><div><h3>New to trading?</h3><p>Meet the tools and try your first task without an account.</p></div>${button("Start here","#start")}</div><article class="next-class panel"><div>${eyebrow('NEXT LIVE CLASS')}<h2>Get oriented</h2><p>Meet your instructor, get comfortable with the workspace and set your practice goal.</p><div class="tags"><span>Date & time to be confirmed</span><span>Proposed · 60 minutes</span></div></div><div class="class-status">Joining details will appear here once the class schedule is set.</div></article><div class="section-head tight"><h2>Your week-one plan</h2><span>${state.complete.size} / 7 practiced</span></div><div class="progress" aria-label="Practice progress"><span style="width:${state.complete.size/7*100}%"></span></div><div class="lesson-list">${lessons.map((x,i)=>`<a class="lesson-row ${state.complete.has(i)?'done':''}" href="#lesson/${i}"><span class="lesson-number">${state.complete.has(i)?'✓':String(i+1).padStart(2,'0')}</span><div><h3>${x.title}</h3><p>${x.summary}</p></div><span>↗</span></a>`).join('')}</div></div><aside><article class="panel accent-panel">${eyebrow(finished?'WEEK ONE PRACTICED':'CONTINUE YOUR PRACTICE')}<span class="large-number">${finished?'✓':'0'+(resume+1)}</span><h3>${l.title}</h3><p>${l.summary}</p>${button(finished?'Review the final session':resume===0?'Begin orientation':'Continue lesson',resume===0?'#start':`#lesson/${resume}`)}</article><article class="panel"><h3>Before your first class</h3><ul class="checklist"><li>Confirm paper practice mode</li><li>Find your chart and Trade panel</li><li>Test sound and internet access</li><li>Write down one learning goal</li></ul><p class="muted">Class link and schedule are not configured yet.</p></article><article class="panel"><h3>Ask a question</h3><p>Bring the step you’re stuck on into the conversation.</p>${button('Write a practice question','#question',true)}</article></aside></section>`}
function lesson(i){if(!lessons[i])return '<section><h1>Lesson not found</h1><a href="#classroom">Back to classroom</a></section>';state.lesson=i;const l=lessons[i];return `<section class="page-heading compact"><a class="back" href="#classroom">← Back to classroom</a>${eyebrow(`WEEK 01 / SESSION 0${i+1}`)}<h1>${l.title}</h1><p>${l.summary}</p>${notice}</section><section class="dashboard"><div class="learning-main"><article class="panel lesson-content"><h2>The idea</h2><p>${l.concept}</p>${practice(i)}<div class="extension"><h3>Go a little deeper</h3><p>${i===0?"Explain the five areas in your own words before moving on.":i===1?"Open the 15-minute comparison below the chart and explain what changes.":i===2?"Compare one and two fictional units using the size selector above.":i===3?"Compare the supplied analysis observation with the example chart.":i===4?"Trace the supplied plan, ticket and close; identify what the result alone cannot tell you.":i===5?"Explain the example calculation if the stop were 97, as described above.":"Explain one limitation of the tools and one question you would bring to class."}</p></div><h2>Check your understanding</h2><p>${l.check}</p><label for="reflection">Your practice notes <span>(temporary, not saved)</span></label><textarea id="reflection" rows="4" placeholder="Write a sample reflection. Do not include personal information."></textarea><button class="button" data-complete="${i}">${state.complete.has(i)?'Practiced ✓':'Mark as practiced'}</button><p class="muted">Marking practice complete records your progress in this preview only.</p></article><div class="actions">${i<6?button('Next session',`#lesson/${i+1}`):button('Return to classroom','#classroom')}${button('Ask about this lesson','#question',true)}</div></div><aside>${i===0?`<article class="panel accent-panel"><h3>First time here?</h3><p>Start with the interactive workspace map. No account or trading knowledge needed.</p>${button("Open the beginner introduction","#start")}</article>`:videoCard(videos[l.video])}<article class="panel"><h3>Plain-language glossary</h3>${glossary()}</article><article class="panel"><h3>Inside a live session</h3><dl class="timing"><dt>Recap</dt><dd>10 min</dd><dt>Concept</dt><dd>15 min</dd><dt>Demonstration</dt><dd>15 min</dd><dt>Practice</dt><dd>15 min</dd><dt>Understanding check</dt><dd>5 min</dd></dl><p class="muted">Proposed format. Dates and times are unset.</p></article></aside></section>`}
function enroll(){const path=location.hash.includes('practicing')?'practicing':'beginner';return `<section class="enrollment"><div>${eyebrow('START WITH A CONVERSATION')}<h1>Your next step<br>starts <em>here.</em></h1><p>Tell us where you’re starting and what you want to understand. That will help shape your learning path when enrollment opens.</p><div class="panel"><h3>What happens next?</h3><ol><li>Share your experience and learning goal.</li><li>Review the confirmed schedule and enrollment terms when available.</li><li>Receive access and joining details after the enrollment process is finalized.</li></ol><p class="muted">Dates, fees, capacity and admission steps are still being decided. This preview does not reserve a place.</p></div></div><form id="enrollment-form" class="panel form-panel" novalidate><span class="badge">DEMO REGISTRATION</span><h2>Find your starting point</h2><p>Use sample details only. Nothing is sent, stored or added to an enrollment list.</p><div id="form-errors" role="alert"></div><div class="form-grid"><label>Sample name<input name="name" autocomplete="off" placeholder="Alex Example" maxlength="80" required></label><label>Sample email<input name="email" type="email" autocomplete="off" placeholder="alex@example.com" maxlength="120" required></label></div><label>Experience level<select name="experience" required><option value="beginner" ${path==='beginner'?'selected':''}>I’m starting with the basics</option><option value="practicing" ${path==='practicing'?'selected':''}>I already trade and want guidance</option></select></label><label>Main difficulty<select name="difficulty" required><option value="">Choose one</option><option>I’m completely new / not sure yet</option><option>Understanding charts and trading terms</option><option>Using Pro Trader confidently</option><option>Following a consistent workflow</option><option>Understanding analysis outputs</option><option>Reviewing my decisions</option></select></label><label>What would you like to learn?<textarea name="goal" rows="3" maxlength="500" placeholder="For example: understand what a chart shows. I’m completely new." required></textarea></label><label class="check"><input type="checkbox" name="demo" required><span>I’m using sample details and understand this is a preview, not enrollment.</span></label><button class="button" type="submit">Preview my next steps <span>↗</span></button></form></section>`}
function question(){return `<section class="narrow page-heading">${eyebrow('ASK A QUESTION')}<h1>Where are you<br><em>getting stuck?</em></h1><p>Describe one step and what you expected to happen.</p><form class="panel" id="question-form"><div class="notice">Demo only. Use a sample question without personal information. It will appear in the teaching preview for this page session; it is not sent to an instructor.</div><label>Related session<select name="lesson">${lessons.map((l,i)=>`<option value="${i}" ${i===state.lesson?'selected':''}>${i+1}. ${l.title}</option>`).join('')}</select></label><label>Sample question<textarea name="question" required minlength="10" maxlength="1000" rows="5" placeholder="Which ticket fields should I check after applying an ARIA signal?"></textarea></label><button class="button">Add to demo questions</button></form></section>`}
function admin(){return `<section class="page-heading compact">${eyebrow('TEACHING PREVIEW')}<h1>Keep the class<br><em>moving forward.</em></h1><div class="notice">Sample teaching view · No authentication or live student records. All materials in this prototype are publicly accessible to anyone with preview access.</div></section><section class="admin"><div class="stats"><article class="panel"><span>Sample learners</span><strong>3</strong></article><article class="panel"><span>Week-one sessions</span><strong>7</strong></article><article class="panel"><span>Demo questions</span><strong>${1+state.questions.length}</strong></article></div><article class="panel"><h2>Student list <span class="badge">SAMPLE DATA</span></h2><div class="table-wrap"><table><thead><tr><th>Learner</th><th>Starting path</th><th>Learning goal</th></tr></thead><tbody><tr><td>Sample learner A</td><td>Foundations</td><td>Explain the practice ticket</td></tr><tr><td>Sample learner B</td><td>Workflow</td><td>Build a consistent review habit</td></tr><tr><td>Sample learner C</td><td>Foundations</td><td>Understand chart timeframes</td></tr></tbody></table></div></article><div class="admin-grid"><article class="panel"><h2>Questions to discuss</h2><div class="question-item"><span class="eyebrow">SAMPLE / PRACTICE TICKET</span><p>Why does the Max Loss estimate change when I change lot size?</p></div>${state.questions.map(q=>`<div class="question-item"><span class="eyebrow">DEMO / ${esc(lessons[q.lesson].title)}</span><p>${esc(q.text)}</p></div>`).join('')}</article><article class="panel"><h2>Session materials</h2>${lessons.map((l,i)=>`<a class="material" href="#lesson/${i}"><span>0${i+1} / ${l.title}</span><span>↗</span></a>`).join('')}<button class="text-button" id="export-content">Download complete teaching packet ↓</button></article></div></section>`}
function render(){const route=location.hash.slice(1).split('?')[0]||'home';main.innerHTML=route==='home'?home():route==='start'?intro(state.complete.has(0)):route==='library'?library():route==='classroom'?classroom():route==='enroll'?enroll():route==='question'?question():route==='admin'?admin():route.startsWith('lesson/')?lesson(Number(route.split('/')[1])):'<section><h1>Page not found</h1><a href="#home">Return to academy</a></section>';document.querySelectorAll('header nav a').forEach(a=>{const active=a.hash==='#'+route;a.toggleAttribute('aria-current',active);if(active)a.setAttribute('aria-current','page')});document.title=`${({home:'Learn with intention',start:'Start here',library:'Video library',classroom:'My classroom',enroll:'Enrollment preview',admin:'Teaching preview',question:'Ask a question'})[route]||'Week-one lesson'} — Pro Trader Academy`;bind();}
function toast(t){document.querySelector('#toast').textContent=t;setTimeout(()=>document.querySelector('#toast').textContent='',4500)}
function bind(){bindPractice(main);
main.querySelector('[data-jump-map]')?.addEventListener('click',()=>{const map=main.querySelector('.workspace-map');map.focus({preventScroll:true});map.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth',block:'start'})});
main.querySelector('[data-intro-complete]')?.addEventListener('click',e=>{state.complete.add(0);state.lesson=1;e.currentTarget.textContent='Introduction practiced ✓';main.querySelector('.intro-progress').textContent='Introduction practiced ✓ · You can revisit any step below.';main.querySelector('.intro-feedback').textContent='Session 1 is marked practiced. Your classroom now points to the chart lesson.';main.querySelector('.actions a[href="#lesson/1"]').textContent='Continue to chart practice ↗';});
main.querySelectorAll('[data-video]').forEach(b=>b.onclick=()=>openVideo(+b.dataset.video));main.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;render();main.querySelector(`[data-filter="${state.filter}"]`).focus()});main.querySelectorAll('[data-complete]').forEach(b=>b.onclick=()=>{state.complete.add(+b.dataset.complete);b.textContent='Practiced ✓';toast('Practice marked complete for this preview.')});const form=document.querySelector('#enrollment-form');if(form)form.onsubmit=e=>{e.preventDefault();const data=new FormData(form),errors=[];form.querySelectorAll('[aria-invalid]').forEach(x=>x.removeAttribute('aria-invalid'));const fail=(name,text)=>{errors.push(text);form.elements[name].setAttribute('aria-invalid','true')};if(!data.get('name').trim())fail('name','Enter a sample name.');if(!form.elements.email.validity.valid||!data.get('email').trim())fail('email','Enter a valid sample email.');if(!data.get('difficulty'))fail('difficulty','Choose your main difficulty.');if(data.get('goal').trim().length<10)fail('goal','Describe a learning goal in at least 10 characters.');if(!data.get('demo'))fail('demo','Confirm that you are using sample details.');if(errors.length){document.querySelector('#form-errors').innerHTML='<p>Please check the following:</p><ul>'+errors.map(x=>`<li>${x}</li>`).join('')+'</ul>';form.querySelector('[aria-invalid]').focus();return}form.innerHTML=`<span class="badge">PREVIEW COMPLETE</span><h2>Your starting point is ${data.get('experience')==='beginner'?'Foundations':'Workflow'}.</h2><p>This is the next-step screen a learner could see. No enrollment was created, no details were saved and no email was sent.</p><div class="extension"><h3>Your next step</h3><p>Explore the orientation lesson while the class schedule and enrollment terms are being finalized.</p></div>${button('Explore your classroom','#classroom')}<p><a href="#enroll" id="restart">Try the form again</a></p>`;form.querySelector('#restart').onclick=e=>{e.preventDefault();render()}};const q=document.querySelector('#question-form');if(q)q.onsubmit=e=>{e.preventDefault();const f=new FormData(q);if(f.get('question').trim().length<10){q.elements.question.setCustomValidity('Use at least 10 characters to describe the step.');q.elements.question.reportValidity();return}state.questions.push({lesson:+f.get('lesson'),text:f.get('question').trim()});q.innerHTML='<h2>Added to demo questions</h2><p>Your sample question is visible in the teaching preview until you refresh. It has not been delivered to anyone.</p>'+button('View demo questions','#admin')};if(q)q.elements.question.oninput=()=>q.elements.question.setCustomValidity('');const ex=document.querySelector('#export-content');if(ex)ex.onclick=()=>{const blob=new Blob([teachingPacket(lessons,videos)],{type:'text/html'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='pro-trader-week-one.html';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}}
function openVideo(id){const v=videos[id],d=document.querySelector('#player');document.querySelector('#player-content').innerHTML=`<video controls autoplay playsinline src="${v.url}"></video><div class="guide">${eyebrow(v.topic+' / '+v.duration)}<h2>${v.title}</h2><p>${v.summary}</p><ol>${v.steps.map(s=>`<li>${s}</li>`).join('')}</ol><div class="extension">${v.note}</div><p class="video-fallback" hidden>The clip could not load. You can still use the written guide below or <a href="${v.url}" target="_blank" rel="noopener">open the original video</a>.</p></div>`;d.querySelector('video').onerror=()=>d.querySelector('.video-fallback').hidden=false;d.showModal()}
document.querySelector('#player .close').onclick=()=>document.querySelector('#player').close();document.querySelector('#player').addEventListener('close',()=>{document.querySelector('#player-content').innerHTML=''});window.addEventListener('hashchange',()=>{render();window.scrollTo(0,0);main.focus({preventScroll:true})});render();
